# Project TODO

A backlog for ideas to revisit, not a commitment to implement them right away.
Add new ideas under the relevant heading or create a new section.

## Extensions

- [ ] Improve the `/extensions` API and controls:
  - [ ] Show which extensions are currently loaded.
  - [ ] Allow extensions loaded by default to be turned on or off.
  - [ ] Give built-in extensions a default include/exclude option.

## Tree

- [ ] Improve the popup tree UI:
  - [ ] Consider making it more similar to the `/resume` window.
  - [ ] Consider a split view with the tree on one side and the conversation on the other.
  - [ ] Automatically scroll the conversation to the selected tree item so it can be seen in context.
